import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors (Purple Theme)
  static const Color primaryDarkPurple = Color(
    0xFF4B2E83,
  ); // Deep purple background
  static const Color primaryLightPurple = Color(
    0xFF8A4AF3,
  ); // Lighter purple (e.g., button, search bar)
  static const Color primaryGradientStart = Color(
    0xFF6B48FF,
  ); // Gradient start (approximated)
  static const Color primaryGradientEnd = Color(
    0xFF9D7BFF,
  ); // Gradient end (approximated)

  // Secondary Colors (Pastel and Neutral)
  static const Color backgroundLight = Color(
    0xFFF5F5F5,
  ); // Light grayish background
  static const Color cardBackground = Color(0xFFFFFFFF); // White for cards
  static const Color textDark = Color(0xFF333333); // Dark text
  static const Color textLight = Color(0xFF666666); // Light text
  static const Color accentPink = Color(
    0xFFFF6B97,
  ); // Pinkish accent (e.g., activity bars)
  static const Color accentBlue = Color(
    0xFF4A90E2,
  ); // Blue accent (e.g., charts)
  static const Color accentGreen = Color(
    0xFF2ECC71,
  ); // Green accent (e.g., check marks)

  // Chart Colors (for Statistics section)
  static const Color chartRed = Color(0xFFE74C3C); // Red bars in activity chart
  static const Color chartBlue = Color(
    0xFF3498DB,
  ); // Blue bars in activity chart
  static const Color chartGray = Color(
    0xFFB0BEC5,
  ); // Gray background or neutral lines

  // Button and Highlight Colors
  static const Color buttonText = Color(0xFFFFFFFF); // White text on buttons
  static const Color shadowColor = Color(
    0x40000000,
  ); // Subtle shadow (semi-transparent black)
}
